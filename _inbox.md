# _inbox — 규칙 후보 수집함 (append-only)

기계 4대 + 동시 세션 여러 개가 `CLAUDE.md`의 **같은 절을 각자 최종 문장으로** 고쳐 쓰다
충돌이 반복됐다. 그래서 **후보와 확정을 분리**한다.

| 파일 | 성격 | 누가 쓰나 |
|---|---|---|
| `CLAUDE.md` | **확정.** 사람이 승인한 규칙만 | 승격 작업 때만 |
| `_inbox.md` (여기) | **후보.** append-only | 아무 세션이나 |

**append-only는 구조적으로 충돌하지 않는다.** 서로 다른 세션이 파일 끝에만 덧붙이면
git이 자동 병합한다. 같은 문장을 고쳐 쓸 때만 충돌한다.

---

## 쓰는 법

- **끝에 덧붙이기만 한다.** 기존 항목을 고치거나 지우거나 순서를 바꾸지 않는다.
- 항목 하나당 아래 형식. 날짜는 절대날짜로 쓴다("어제" 금지).
- **결론이 아니라 관측을 쓴다.** 최종 문장을 다듬는 것은 승격 때 한 세션이 한다.
- 적은 뒤 커밋 메시지는 `inbox: <한 줄>`.

```markdown
### 2026-08-06 / lgcns-server / <한 줄 제목>
- **관측**: 무슨 일이 실제로 있었나 (추측 말고 확인된 것)
- **후보 규칙**: 이렇게 하면 재발을 막을 수 있다
- **넣을 곳**: CLAUDE.md 어느 절 / 어느 스킬 / 메모리 / 프로젝트 문서
```

## 승격 (inbox → CLAUDE.md)

- 시점: **세션종료준비** 4번 항목을 수행할 때, 또는 항목이 쌓였을 때
- 한 번에 **한 세션만** 한다. 여러 세션이 동시에 승격하면 그게 곧 충돌이다
- 절차: 여기 있는 항목을 읽고 → 중복·모순 정리 → `CLAUDE.md` 해당 절에 반영 →
  **승격한 항목은 아래 「승격 완료」로 옮긴다**(지우지 않는다. 기각 이력도 남긴다)
- ⚠️ **기각도 반드시 기록한다.** 남기지 않으면 다음 세션이 같은 것을 다시 올린다

---

## 승격 완료 / 기각

승격·기각된 항목을 결과와 함께 여기로 옮겨 적는다. **아직 없음.**

---

## 미승격

⚠️ **새 항목은 반드시 이 파일의 맨 끝에 붙인다.** 이 절이 마지막에 오는 이유가 그것이다.
중간에 끼워 넣으면 append-only가 깨져 충돌한다.

### 2026-08-06 / lgcns-server / 노션 커넥터는 워크스페이스 전체가 열려 있다

- **관측**: 노션 MCP 커넥터로 제목 검색 한 번에 개인 페이지(`기억할 내용들`)가 그대로 잡혔다.
  사용자는 그 페이지를 Claude가 못 보게 하고 싶어 했다. 접근 경로가 **두 개**로 나뉜다는 점이
  드러났다: MCP 커넥터(OAuth, 워크스페이스 전반)와 REST 토큰(내부 통합, 명시 연결 페이지만).
  `notion-sync` 스킬 7절은 후자만 다루고 있어 전자를 놓쳤다.
- **관측 2**: 커넥터의 서버 식별자가 UUID인데 `~/.claude.json`·`settings.json` 어디에도
  저장돼 있지 않다. 세션마다 부여되는 값이면 그 UUID로 건 `permissions.deny` 규칙은
  다음 세션에서 안 맞는다. **미검증** — 새 세션에서 UUID가 같은지 확인해야 한다.
- **후보 규칙**: 노션 관련 작업을 안내할 때 "통합에 연결된 페이지만 보인다"고 말하지 않는다.
  커넥터 경로가 따로 있고 그쪽은 범위가 넓다. 차단을 안내할 때는 **두 경로를 모두** 짚는다.
- **넣을 곳**: `notion-sync` 스킬 7절 (접근 경로 구분 추가)

### 2026-08-06 / lgcns-server / MCP 도구 식별자로 건 설정은 지속성을 확인해야 한다

- **관측**: 위 UUID 건과 같은 뿌리. 디스크에 없는 식별자에 규칙을 걸면 조용히 무효화된다.
  에러가 나지 않으므로 "차단했다"고 믿은 채로 지나간다.
- **후보 규칙**: 설정에 식별자를 하드코딩하기 전에 그 값이 **디스크의 설정 파일에 있는지**
  확인한다. 없으면 런타임에 부여되는 값일 수 있으므로, 적용 후 **다음 세션에서 재검증**하고
  그때까지는 "차단됨"이라고 단정하지 않는다.
- **넣을 곳**: `CLAUDE.md` 조사 규율(4절) 또는 별도 절

### 2026-08-06 / lgcns-server / 키 이름이 같다고 사본이라 단정하지 말 것 — 값을 대조하라

- **관측**: IRS 저장소 정리 중 루트의 `env.txt`를 `ESOC/Ops/MIB/.env`의 오래된 사본으로 보고
  **삭제 후보로 보고했다.** 근거는 "키 이름 11개가 전부 `.env`에도 있다"였다. 사용자가
  "삭제하면 DB 접속은 어떻게 관리하냐"고 되물어 값을 대조해 보니 **11개 중 5개의 값이 달랐다**
  (호스트·계정·비밀번호). `.env`는 DVC/SPC 호스트가 동일한 **로컬 Docker**용이고 `env.txt`는
  서로 다른 호스트를 가리키는 **운영 접속정보**였다. 지웠으면 운영 접속 수단을 잃을 뻔했다.
- **관측 2**: 값을 직접 출력하지 않고도 판별할 수 있었다 — 키별 sha256 앞자리 비교, 그리고
  "`.env` 안에서 DVC_HOST == SPC_HOST 인가" 같은 **파일 내부 관계**를 물으면 성격이 드러난다.
- **후보 규칙**: 두 설정 파일이 겹쳐 보일 때 **키 집합만으로 중복이라 판정하지 않는다.**
  값을 대조하되 자격증명은 평문 출력 없이 해시·길이·파일 내부 관계로 비교한다.
  삭제 후보로 올리기 전에 **"이 파일을 읽는 코드가 있는가"와 "값이 같은가"를 둘 다** 확인한다.
- **넣을 곳**: `CLAUDE.md` 조사 규율(4절) — "소스를 열기 전에 진단하지 않는다"의 파생 사례

### 2026-08-06 / lgcns-server / 승인받는 작업은 실측한 범위를 제시할 것

- **관측**: `.gitattributes` 도입 후 줄바꿈 정규화를 제안하며 "docs 5개 파일 2,533줄"이라고
  범위를 적어 사용자 승인을 받았다. 실제로 `git add --renormalize .`를 실행하니
  **99개 파일 66,275줄**이었다(csv 40·yaml 21·sql 21·md 7 등). docs 디렉터리만 실측하고
  나머지는 추정 없이 넘어간 것이 원인. 커밋 전에 범위를 다시 보고해 바로잡았으나,
  사용자가 승인한 규모와 실제 규모가 20배 차이였다.
- **후보 규칙**: 사용자 승인을 받는 작업은 **승인 요청 전에 dry-run 등으로 전체 영향 범위를
  실측**한다. 일부만 측정하고 전체를 추정해 숫자로 제시하지 않는다. 실측이 어려우면
  "정확한 범위는 실행해 봐야 안다"고 밝히고 승인받는다.
- **넣을 곳**: `CLAUDE.md` 조사 규율(4절) 또는 파괴적 명령 절

### 2026-08-06 / lgcns-server / .git 이 비대할 때 원인 특정 절차

- **관측**: IRS 저장소 `.git`이 1.2GB인데 정상 이력은 71MB뿐이었다. 원인은 **팩에 남은
  도달 불가 객체 1개**(1.16GB zip). `git add` 후 커밋이 취소되면 객체는 이미 기록된 뒤라
  이력에 안 보이면서 디스크는 계속 차지한다. `git log`·`git status`로는 전혀 안 보인다.
- **절차** (재사용 가치가 있어 남긴다):
  1. `git count-objects -vH` → `size-pack`이 실제 사용량. `in-pack` 개수도 함께 본다
  2. `git verify-pack -v .git/objects/pack/*.idx | grep -E '^[0-9a-f]{40}' | sort -k3 -rn | head`
     → 팩 내 최대 객체
  3. `git rev-list --objects --all | grep <sha>` → 출력이 없으면 **고아**
  4. 정상 이력 크기: `git rev-list --objects --all | git cat-file --batch-check=... | awk` 합산
  5. 1번과 4번의 차이가 곧 고아 용량. 제거는 `git reflog expire --expire=now --all && git gc --prune=now`
- **주의**: 고아 객체가 **이 기계의 유일본일 수 있다.** 삭제 전에 내용을 식별하고
  (zip이면 중앙 디렉터리만 파싱해 파일 목록 확인) 원본이 디스크 어딘가에 있는지 찾는다.
  실제로 이번 건은 개인 자료였고 원본 폴더가 이미 삭제돼 팩이 유일본이었다.
- **넣을 곳**: 별도 스킬(git 정비) 또는 `CLAUDE.md` 참고 절

### 2026-08-06 / lgcns-server / 사용자가 준 숫자는 단위부터 확정할 것

- **관측**: 신혼집 프로젝트에서 사용자가 "아내 부모님 나이는 부(59), 모(60)"이라고 적었다.
  이를 **만 나이로 읽고** "아버지는 60세 미만이라 주택공급규칙 제53조 제6호(60세 이상 직계존속
  주택 제외)가 적용 안 됨 → 부모님 거주 주택의 명의가 청약 무주택 판정을 가른다"는 판정을 세워
  프로젝트 문서 3곳과 노션 페이지에 적재했다. 다음 턴에 사용자가 "60과 59는 출생년도다"라고
  하여 **두 분 다 만 65세 초과**로 뒤집혔고 판정 전체를 되돌렸다.
- **관측 2**: 같은 세션에서 신혼희망타운 총자산 기준을 기억에 의존해 "3억대 후반"이라고 썼다가
  LH 공식 페이지 확인 결과 **362,000,000원**이었다. 결론(자산 초과로 탈락)은 같았으나
  근거 수치가 틀린 채로 노션에 한 번 적재됐다.
- **후보 규칙 A**: 사용자가 준 숫자는 **단위·기준을 확정한 뒤** 판정에 쓴다. 연도인지 나이인지,
  만 나이인지 세는 나이인지, 원인지 만원인지. 애매하면 되묻는다 — 한 번 묻는 비용이
  판정을 뒤집고 적재본을 되돌리는 비용보다 훨씬 싸다.
- **후보 규칙 B**: **법령 기준액·세율·요건 수치는 기억에서 꺼내지 않는다.** 원문(law.go.kr)이나
  소관 기관 페이지로 확인하고 출처 링크를 산출물에 남긴다. `claude-api` 스킬의
  "모델 카탈로그·가격을 기억에서 꺼내지 말 것"과 같은 패턴이며, 대상만 다르다.
- **넣을 곳**: `CLAUDE.md` 조사 규율(4절) — "소스를 열기 전에 진단하지 않는다"의 파생.
  규칙 B는 별도 항목으로 세워도 될 만큼 재발 빈도가 높아 보인다.

### 2026-08-07 / lgcns-server / 규정과 권장을 문장에서 구분할 것

- **관측**: 신혼집 프로젝트에서 취득세·세대분리 절차를 설명하며 **방어적 권고를 법적 요건인 것처럼**
  쓴 것이 한 세션에 세 번 나왔다. 세 번 다 사용자가 되물어서 드러났다.
  - "남편 전입 시점을 아내 매수 이후로 맞추면 됩니다" → 실제로는 순서 제약이 아니라
    두 집 유지 비용을 줄이자는 얘기였다. 혼인신고 전이라 두 사람 세대는 서로 무관하다.
  - "각자 세대주로 분리 등재" → 혼인 전이면 법적으로 가족이 아니라 애초에 세대 합산이 안 된다.
    불필요한 절차를 넣었다. 사용자가 "남편은 지금 집에 있어도 되잖아?"라고 지적해 철회했다.
  - "증여를 계약 전에 받아야" → 자금조달계획서는 계약 후 30일 이내 제출이라 잔금 전이면 된다.
- **관측 2**: 세 건 모두 **결론 자체는 안전한 쪽**이었다. 그래서 틀렸다는 신호가 안 났고,
  사용자가 묻지 않았으면 불필요한 이사·전입·자금 일정을 그대로 실행했을 것이다.
  안전한 오답이 위험한 오답보다 발견이 늦다.
- **후보 규칙**: 절차를 안내할 때 **"규정이라 반드시 그래야 하는 것"과 "이렇게 하면 더 안전한 것"을
  문장에서 구분**한다. 전자는 근거(법령·조문·약정)를 함께 적고, 후자는 "권장" "안전하게 하려면"
  같은 표지를 붙인다. 표지 없이 섞어 쓰면 상대는 전부 필수로 받아들인다.
- **넣을 곳**: `CLAUDE.md` 문서 작성 규칙 절. 기존 "'권장' 표현 지양" 항목과 방향이 반대로 보이지만
  실제로는 짝이다 — 그 항목은 **단정을 피하라**는 것이고 이건 **필수와 권장을 구분하라**는 것이다.
  승격 시 두 항목을 함께 다듬는 편이 낫다.

### 2026-08-06 / mac / iCloud dataless 파일은 용량뿐 아니라 '열림' 자체가 안 된다

- **관측**: iCloud Drive의 한 프로젝트 폴더(파일 112개)가 전부 로컬 블록 0이었다.
  `du -sh`는 64K인데 개별 파일은 16MB로 표시된다. 최신 macOS는 `.icloud` 확장자를
  붙이지 않아 `ls`·Finder에서 정상 파일과 구별되지 않는다. 확실한 판별은
  `stat -f "%z %b"`로 논리크기와 할당 블록을 함께 보는 것이었다(블록 0 = 미다운로드).
- **관측 2**: 4절의 "용량 측정은 논리크기와 실제블록을 구분한다" 항목은 **용량 오측**만
  다룬다. 실제로 더 아픈 것은 **가용성**이다. dataless 파일은 오프라인에서 열리지 않는다.
  이 사용자는 2026-11-14 예식 당일 식장에서 체크리스트 파일을 열어야 하는데, 전파가
  나쁘면 파일이 없는 것과 같다.
- **후보 규칙**: 4절 기존 항목에 두 줄 보탠다. ① `.icloud` 확장자가 없으므로 겉보기로
  판별하지 말고 `stat -f "%z %b"`의 블록 수를 본다. ② 현장·오프라인에서 써야 할 파일은
  미리 "지금 다운로드"로 고정한다. 산출물을 작은 단일 파일(HTML 등)로 만들면 영향이 작다.
- **넣을 곳**: `CLAUDE.md` 4절, 기존 "용량 측정" 항목에 병합.

### 2026-08-06 / mac / `~/.claude/CLAUDE.md`는 stub이 아니라 '머신 서술' 자리다

- **관측**: `README.md` 146행은 `~/.claude/CLAUDE.md`를 "한 줄짜리 stub"이라 적었는데,
  이 mac의 파일에는 머신 사양·디스크 여유·데이터 취급 원칙·서버 위임 규칙이 들어 있었다.
  stub으로 간주하고 덮어쓰면 그 내용이 날아간다(README 53행이 같은 위험을 이미 경고한다).
- **관측 2**: 이번 세션에서 iCloud 판별법을 머신 파일에 넣었다가, 그건 도서관 Mac에서도
  쓸 지식이라 공유 쪽에 있어야 한다는 지적을 받고 옮겼다. 경계가 문서화돼 있지 않아
  생긴 왕복이다.
- **후보 규칙**: 역할을 명문화한다. **어느 기계에서나 쓰는 방법·판단 기준은
  claude-config(git 동기화), 그 기계의 상태·제약은 `~/.claude/CLAUDE.md`.**
  머신 파일에는 사실만 적고 방법은 claude-config를 가리킨다
  (예: "iCloud 저장공간 최적화 On. 판별법은 claude-config 4절 참조").
- **넣을 곳**: `README.md`의 "stub" 서술 정정 + `CLAUDE.md` 머리말에 한 줄.

### 2026-08-08 / mac / GUI 앱은 "열기"만으로 원본 파일을 변형한다

- **관측**: 읽기 전용으로 볼 생각이던 보드 파일 4벌을 KiCad 10으로 열었다. 저장 버튼을
  누른 적이 없는데도 프로젝트 파일(`.kicad_pro`) 4개가 신형 포맷으로 **덮어써졌고**,
  뷰 설정 파일(`.kicad_prl`) 4개와 **remote 없는 git 저장소(`.history/`)** 가 새로 생겼다.
  그 저장소에는 보드 사본이 원본(351KB)이 아니라 변환본(691KB)으로 커밋돼 있었다
  (author `KiCad <noreply@kicad.org>`, message "Autosave").
- **관측 2**: 사용자에게 "저장하지 마세요"라고 안내해 두었고 실제로 저장하지 않았다.
  그런데도 위 변경이 전부 일어났다. **"저장 금지"는 무결성 보장이 아니다.**
- **관측 3**: 원본 tar를 별도로 다시 풀어 대조한 뒤에야 무엇이 바뀌었는지 확정할 수 있었다.
  대조 전에는 "안 건드렸다"고 믿고 있었다. 실제 보드 데이터(`.kicad_pcb`)는 SHA256이
  일치해 무사했지만, 그건 대조해서 안 것이지 안전하다고 알고 있던 게 아니다.
- **후보 규칙**: 원본·고객 데이터를 GUI 앱으로 열기 전에 **되돌릴 사본(tar 등)을 확보하고
  체크섬을 떠 둔다.** 작업 후 재귀 비교로 무엇이 바뀌었는지 확정해 보고한다.
  "열기만 할 것"이라는 이유로 이 절차를 건너뛰지 않는다.
- **넣을 곳**: `CLAUDE.md` 4절(조사 규율) 또는 파괴적 명령 절 옆에 신규 항목.

### 2026-08-08 / mac / 100%·0% 같은 극단값은 발견이 아니라 계측 오류의 징후다

- **관측**: 보드 파일에서 부품 회전 각도를 집계했더니 4개 세대 전부 **"269개 중 269개가
  0도"** 로 나왔다. 문서에는 세대별로 회전이 달라졌다고 적혀 있어 모순이었다.
- **관측 2**: 확인해 보니 이 생성기가 만든 파일은 `(at x y)` 각도 필드를 **아예 쓰지 않고**,
  회전을 패드·코트야드 로컬 좌표에 미리 구워 넣고 있었다. 즉 각도 필드는 값이 0인 게
  아니라 **처음부터 비어 있던 것**이고, 나는 없는 필드를 세고 있었다.
- **관측 3**: 같은 부품을 기준판과 패드 배치로 대조하는 방식으로 다시 재니 실제 수치가
  나왔다(한 세대는 53개가 90° 불일치, 다음 세대는 0개). 문서에 적힌 비율과도 달랐다.
- **후보 규칙**: 지표가 **정확히 100% 또는 0%** 로 떨어지면 결론을 내기 전에
  **측정 대상이 맞는지부터 의심한다.** 특히 "필드가 비어 있음"과 "값이 0"은 다르다 —
  파서가 없는 필드를 기본값으로 채우고 있지 않은지 원문을 눈으로 확인한다.
  4절의 "검증 수치가 나쁘게 나오면 데이터보다 먼저 검사 자체를 의심한다"의 반대 방향
  사례다(그쪽은 낮은 수치, 이쪽은 완벽한 수치).
- **넣을 곳**: `CLAUDE.md` 4절, "검증 수치가 나쁘게 나오면…" 항목 바로 뒤에 병합.

### 2026-08-08 / mac / 다른 기계의 "컨텍스트에 없다"를 로딩 실패로 진단하기 전에 체크아웃 신선도를 본다

- **관측**: `CLAUDE.md`를 `rules/` 6개로 분할한 뒤, 다른 기계(회사서버)에서 6문항으로
  로드 검증을 했더니 **1개만 "모름"**이 나왔다. 그 1개가 `rules/workflow.md` 담당 문항이라
  "그 파일만 import에 실패했다"는 가설이 먼저 떠올랐다.
- **관측 2**: 실제 원인은 분할과 무관했다. 그 기계의 체크아웃이 `c2d26d0`에 머물러 있었고,
  해당 내용(du/stat 규칙)은 그보다 **나중 커밋(`d3a5a5e`)에서 들어온 것**이었다. pull 하니
  `_inbox.md`·`install.sh`·훅 3개·스킬까지 한꺼번에 딸려 왔다. 여러 커밋 뒤처져 있었다.
- **관측 3**: 정답을 맞힌 나머지 5문항은 전부 훨씬 오래된 내용이었다. 즉 **"최근 추가분만
  정확히 빠지는" 패턴**이 낡은 체크아웃의 지문이었는데, 처음엔 그게 보이지 않았다.
- **후보 규칙**: 다른 기계가 "컨텍스트에 그 내용이 없다"고 답해도 **규칙 로딩 실패로 바로
  단정하지 않는다.** `git log --oneline -1`로 체크아웃 신선도를 먼저 확인한다.
  그리고 **검증 질문은 대상 파일의 '오래된' 내용으로 고른다** — 최근 추가분으로 물으면
  체크아웃 신선도와 import 성공 여부가 뒤섞여 구분되지 않는다. 두 가설이 같은 증상을
  내는 상황에서는 **둘을 갈라내는 질문을 따로 설계**해야 한다.
- **넣을 곳**: `rules/workflow.md` 「조사 규율」. "소스를 열기 전에 진단하지 않는다" 항목의
  기계 간 버전 차이 사례로 붙이면 맞는다.
